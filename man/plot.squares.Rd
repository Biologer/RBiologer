% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biologer.R
\name{plot.squares}
\alias{plot.squares}
\title{Plot Observations Within EEA Grid Square(s)}
\usage{
\method{plot}{squares}(
  fill = "taxon.name",
  shape = NULL,
  grid_type = c("1km", "10km"),
  square_id = NULL,
  df,
  layer = "zupanije",
  zupanija = NULL,
  padding = 0,
  title = NULL,
  x_axis_label = NULL,
  y_axis_label = NULL,
  legend_fill_title = NULL,
  legend_shape_title = NULL,
  dot_size = 2,
  layer_fill_color = "lightblue",
  layer_border_color = "black",
  layer_border_width = 0.5,
  grid_fill_color = "orange",
  grid_border_color = "red",
  grid_border_width = 1,
  palette = "viridis"
)
}
\arguments{
\item{fill}{Column name for coloring points. Default is "taxon.name".}

\item{shape}{Column name for point shapes. Default is NULL (no shapes). If provided, this
column must exist in \code{df}; otherwise shape aesthetics are omitted.}

\item{grid_type}{Which grid layer to use: "1km" or "10km". Default is "1km".}

\item{square_id}{A vector of square IDs to filter and plot (e.g., c("10kmE459N247", "10kmE460N244")).
Default is NULL (plots all squares in the chosen grid).}

\item{df}{A data frame containing \strong{at least} \code{longitude} and \code{latitude} columns, plus
any additional columns you want for \code{fill} or \code{shape}.}

\item{layer}{Map layer to use for the background: "rh" or "zupanije". Default is "zupanije".}

\item{zupanija}{Vector of Zupanija names to filter and plot. Default is NULL (plots all).}

\item{padding}{Padding in meters around the selected square(s) to adjust the map extent. Default is 0.}

\item{title}{Title for the plot with observations. Default is NULL.}

\item{x_axis_label}{Label for the X-axis. Default is NULL.}

\item{y_axis_label}{Label for the Y-axis. Default is NULL.}

\item{legend_fill_title}{Title for the fill legend. Default is NULL.}

\item{legend_shape_title}{Title for the shape legend. Default is NULL.}

\item{dot_size}{Size of the observation points. Default is 2.}

\item{layer_fill_color}{Fill color for the map layer. Default is "lightblue".}

\item{layer_border_color}{Border color for the map layer. Default is "black".}

\item{layer_border_width}{Border width for the map layer. Default is 0.5.}

\item{grid_fill_color}{Fill color for the selected grid squares. Default is "orange".}

\item{grid_border_color}{Border color for the selected grid squares. Default is "red".}

\item{grid_border_width}{Border width for the selected grid squares. Default is 1.}

\item{palette}{Color palette for points. Can be a predefined palette (e.g., "viridis", "plasma",
"rainbow") or a custom named vector. Default is "viridis".}
}
\value{
A list containing two ggplot objects:
\itemize{
\item \code{plot_with_points}: Plot showing the observations (if any) within the selected square(s).
\item \code{square_location}: Plot showing only the selected square(s) over the chosen map layer.
}
}
\description{
This function plots observations within user-selected EEA grid square(s). It provides both
a detailed plot with observations (if any fall within the squares) and a location overview
of the selected squares.
}
\examples{
\dontrun{
# Example data
my_data <- data.frame(
  taxon.name = c("Species A", "Species B"),
  longitude  = c(16.0, 15.5),
  latitude   = c(45.8, 45.9),
  shape_col  = c("Circle", "Triangle") # Example shape column
)

# Plot single 10×10 km square with shape
single_10km <- plot.squares(
  fill               = "taxon.name",
  shape              = "shape_col",
  grid_type          = "10km",
  square_id          = c("10kmE459N248"),
  df                 = my_data,
  layer              = "zupanije",
  zupanija           = c("Zagreb"),
  padding            = 5000,
  title              = "Single 10×10 km Square",
  x_axis_label       = "Longitude",
  y_axis_label       = "Latitude",
  legend_fill_title  = "Taxon Legend",
  legend_shape_title = "Shape Legend",
  dot_size           = 3,
  layer_fill_color   = "grey90",
  layer_border_color = "blue",
  layer_border_width = 0.8,
  grid_fill_color    = "yellow",
  grid_border_color  = "red",
  grid_border_width  = 1.5,
  palette            = "plasma"
)

# Access the plots
single_10km$plot_with_points
single_10km$square_location
}
}
